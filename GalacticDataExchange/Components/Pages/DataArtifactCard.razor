@using GalacticDataExchange.Shared;

@inject NavigationManager NavigationManager

<div class="container my-5">
	<div class="card shadow-sm m-2" style="cursor:pointer" @onclick="@(() => NavigationManager.NavigateTo($"/artifact/{Artifact.Id}"))">

		@if (!string.IsNullOrWhiteSpace(Artifact.ImageURL))
		{
				<img src="@Artifact.ImageURL" class="card-img-top" style="height: 140px; object-fit:cover;" alt="Data Artifact Image" />
		}
		else if (!string.IsNullOrWhiteSpace(Artifact.VideoURL))
		{
				<video class="card-img-top" style="height:  140px; object-fit: cover;" muted autoplay loop playsinline">
					<source src="@Artifact.VideoURL" type="video/mp4" />
					Your browser does not support the video tag.
				</video>
		}
		else
		{
				<img src="/images/xelvorr.png" class="card-img-top" alt="Placeholder Image" />
		}

		<div class="card-body">
			<h5 class="card-title">@Artifact.Name</h5>
			<p class="card-text text-muted">@Artifact.DataArtifactTypeID</p>
			<p class="card-text text-muted">@Artifact.TranslatedText</p>
			<span class="badge bg-light text-dark">♥</span>
		</div>
	</div>
</div>

@code {

	[Parameter] public DataArtifactViewModel Artifact { get; set; } = default!;
}
